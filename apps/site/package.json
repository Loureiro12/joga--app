{
  "name": "jogae-site",
  "version": "0.1.0",
  "private": true,
  "description": "jogae.app — landing, páginas de convite (deep links), páginas legais e arquivos .well-known.",
  "type": "module",
  "engines": {
    "node": ">=20.3.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "node ./dist/server/entry.mjs",
    "check": "astro check && astro build && node scripts/smoke.mjs",
    "og": "./scripts/generate-og.sh"
  },
  "dependencies": {
    "@astrojs/node": "^9.5.5",
    "@astrojs/vercel": "^8.2.11",
    "astro": "^5.18.2"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "typescript": "^5.9.2"
  }
}
