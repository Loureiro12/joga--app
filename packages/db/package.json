{
  "name": "@jogae/db",
  "version": "0.1.0",
  "private": true,
  "description": "Tipos do banco gerados pelo Supabase CLI (`npm run db:types`). Não edite à mão.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "sideEffects": false,
  "scripts": { "typecheck": "tsc --noEmit" },
  "devDependencies": { "typescript": "~6.0.3" }
}
